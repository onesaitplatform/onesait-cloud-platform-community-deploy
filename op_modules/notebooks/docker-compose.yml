version: "2.1"

services:
  zeppelin:
    networks:
      - op_data_datanetwork
    container_name: zeppelin
    image: ${REPOSITORY}/onesaitplatform/zeppelin:${MODULE_TAG}
networks:
  op_data_datanetwork:
    external: true
